Phase gate:
- Generate `design.md` only after explicit requirements approval, unless the user explicitly requested all phases in one pass.
- After design, stop for approval before tasks unless the user already selected `design then auto tasks`.
