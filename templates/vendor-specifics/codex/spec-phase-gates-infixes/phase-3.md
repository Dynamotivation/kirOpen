Phase gate:
- Generate `tasks.md` only after explicit design approval.
- Exception: if the user explicitly selected `design then auto tasks` after requirements approval, generate tasks immediately after design in that same continuation.
