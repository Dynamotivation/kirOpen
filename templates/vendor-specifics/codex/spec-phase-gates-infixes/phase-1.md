Phase gate:
- In default flow, stop after `requirements.md`.
- Ask for explicit continuation before design using:
  - `approve requirements` to continue to design.
  - `approve requirements + design then auto tasks` to run design and then tasks in order without another confirmation between those two phases.
