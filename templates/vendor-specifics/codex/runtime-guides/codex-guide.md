# KirOpen On Codex: Runtime Guide

Generated by: `assemble_instructions.py`  
Builder version: `{{BUILDER_VERSION}}`

This guide summarizes Codex behavior for KirOpen users and provides practical recovery steps for common quirks.

## Mapping Table

| KirOpen Concept | Codex Surface | Notes |
|---|---|---|
| Spec workflow | `.kiro/specs/<feature>/requirements.md`, `design.md`, `tasks.md` | Structured spec artifacts remain under `.kiro/specs` |
| Spec specialist agent | `.codex/agents/spec_mode.toml` | Use `@spec_mode` fallback when routing is not behaving as expected |
| Reusable workflows | `.agents/skills/*/SKILL.md` | Skills remain reusable and cross-request |
| Global repository guidance | `CODEX.md` (or `AGENTS.md` in Codex-only default mode) | Chosen by output mode/options |
| Directory-scoped guidance | `AGENTS.override.md` in relevant folders | Use for situational context by directory |
| Porting legacy Kiro config | `port-kiro-configuration-to-kiropen-on-codex` skill | Translates intent to Codex-native outputs |
| Codex Plan mode | Harness planning feature | Competes with Spec mode intent; choose one explicitly |

## Quirks and Playbooks

### 1) Plan Mode Competes With Spec Mode

Symptom: Codex Plan mode is engaged even though you intended KirOpen Spec mode workflow.

Do this:
1. Send: `Use Spec mode workflow, not Codex Plan mode for this request.`
2. If needed, invoke directly: `@spec_mode Continue in spec mode.`
3. After each phase, tell the agent what you want next — continue, revise, or skip ahead.

### 2) Spec Mode Auto Progression

Symptom: assistant progresses requirements -> design -> tasks without waiting for approvals.

Do this:
1. Send: `Stop after requirements only. Wait for my approval before moving to design.`
2. After each phase, explicitly tell the agent to continue, revise, or generate the remaining phases together.

### 3) Spec Intent Not Routed As Expected

Symptom: spec request is handled inline when you expected specialist workflow handling.

Do this:
1. Send: `@spec_mode Continue this in spec mode.`
2. If still ambiguous, add: `Use requirements first, then wait for approval.`

### 4) Higher-Level Policy Overrides Template Behavior

Symptom: behavior conflicts with repository templates (for example stricter delegation policy).

Why: active runtime/session instructions can override repo-level template content.

Do this:
1. Treat runtime policy as authoritative for that session.
2. Use explicit consent phrasing in the chat (`I explicitly want spec_mode for this request.`).
3. If needed, use direct invocation fallback (`@spec_mode`).

## Limits

- This guide documents behavior; it does not change runtime policy.
- Runtime-level instructions may supersede repository templates.
